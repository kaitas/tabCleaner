# 2026-01-12 開発ログ & アーキテクチャ設計

## 📝 開発ログ
- **v0.2**: タブ選択削除機能（Review UI）を実装完了。
- **v0.3**: FTUE（初回セットアップ）、Webサイト（多言語対応）、CI/CD（リリース自動化）を実装完了。
- **v0.4 (In Progress)**:
  - コンセプト刷新: "The Browser Angel" (天使) と "Distributed Gamification" (分散ゲーミフィケーション)。
  - プライバシー重視: ローカル主権、GDPR準拠。
  - 運用コストゼロ: Serverlessアーキテクチャの採用。

## 🏗 アーキテクチャ設計 (Zero Cost / Immortal)

### 概要
「作者がいなくなっても動き続ける」ことを目標に、サーバー運用費のかからない構成を採用する。

### 構成要素
1.  **Frontend (Extension)**
    -   Platform: Chrome Extension Manifest V3
    -   Language: Vanilla JS (No build step required for core logic)
    -   Logic:
        -   `chrome.alarms` による定期実行（タブ収集）。
        -   `chrome.storage.local` によるデータ永続化。
        -   Google Spreadsheet API (via GAS) による個人履歴バックアップ。

2.  **Web / Documentation**
    -   Platform: **GitHub Pages**
    -   Content: LP, HowTo, Privacy Policy, ToS.
    -   Domain: Custom domain or `kai-tas.github.io/tabCleaner`.

3.  **Global Stats (Backend)**
    -   **Ingress**: **Cloudflare Workers** (Free Plan)
        -   機能: ユーザーからの匿名化統計データの受信、検証、R2への書き込み。
        -   制限: 100k requests/day (十分な枠)。
    -   **Storage**: **Cloudflare R2** (Free Plan)
        -   機能: 生ログの保存、集計済みJSONのホスティング。
        -   コスト: 10GBまで無料。Class A操作の制限に注意して設計する。
    -   **Visualization**: GitHub Pages上のJSが R2 (public bucket) からJSONをfetchして描画。

### データの流れ
1.  Extension -> (Anonymized JSON) -> Cloudflare Worker
2.  Worker -> (Validation & Sanitize) -> R2 Bucket
3.  GitHub Actions (Cron) -> (Aggregation Logic) -> R2 Bucket (`global_stats.json`)
4.  Website (JS) <- (Fetch) <- R2 Bucket (`global_stats.json`)

### プライバシー & セキュリティ
- **匿名化**: 送信データには `UUID` (ランダム生成) のみを含め、個人を特定可能な情報（IPアドレス等）はWorker側で破棄またはマスクする。
- **合意形成**: インストール時 (FTUE) に明示的なデータ送信の同意（Opt-in）を取得する。同意しない場合、通信は一切行わない。
